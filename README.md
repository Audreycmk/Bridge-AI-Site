# Bridge AI Landing Site

A modern Next.js landing page for Bridge AI, built with a brand-driven look and a polished, tech-forward interface.

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```
2. Start the development server:
   ```bash
   npm run dev
   ```
3. Open `http://localhost:3000` in your browser.